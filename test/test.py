book_title = ['great', 'expectations', 'the',
                 'adventures', 'of', 'sherlock',
                  'holmes', 'the', 'great',
                   'gasby', 'hamlet', 'adventures',
                    'of', 'huckleberry', 'fin'
            ]

result_dict = {}
for title in book_title:

        book_title.count
        # if title in result_dict:
        #         # result_dict[title] = title.count
        #         result_dict.count()
        # else:
        #         result_dict[title] = 1


print(result_dict)